#:schema https://developers.openai.com/codex/config-schema.json

model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
service_tier = "fast"
model_verbosity = "low"

# Runtime selections made by the user still take precedence.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
hooks = true
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 2
max_depth = 1
default_subagent_model = "gpt-5.6-sol"
default_subagent_reasoning_effort = "ultra"
interrupt_message = true

# Override every general-purpose spawn role as read-only. The root session is
# the only writer; subagents inspect, challenge, and report.
[agents.default]
description = "Read-only general checker; never writes or publishes."
config_file = "agents/default.toml"

[agents.worker]
description = "Read-only worker override; implementation delegation is disabled for Cogito++."
config_file = "agents/worker.toml"

[agents.explorer]
description = "Read-only codebase explorer for bounded evidence gathering."
config_file = "agents/explorer.toml"

# Optional reviewers for ambiguous or high-risk work. They are not mandatory
# stage gates.
[agents.contract_reviewer]
description = "Optional read-only reviewer for Gemini-authored contracts and implementation scope."
config_file = "agents/contract-auditor.toml"

[agents.implementation_verifier]
description = "Optional independent read-only verifier for completed implementation work."
config_file = "agents/exit-gate-verifier.toml"

# Documentation-only MCP. GitHub metadata keeps using the connected app.
[mcp_servers.openaiDeveloperDocs]
url = "https://developers.openai.com/mcp"
enabled = true
required = false
default_tools_approval_mode = "auto"
startup_timeout_sec = 15
tool_timeout_sec = 60
